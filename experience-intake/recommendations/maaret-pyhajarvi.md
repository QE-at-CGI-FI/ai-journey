# Recommendations for Maaret Pyhäjärvi

Based on the mapping logged 2026-09-25 (see `coverage-log.md`): 24/29 items
on — full 12/12 on the knowledge-worker side, 12/17 on the developer side.
The five items still off (`team-product-context-basic`, `building-own-mcps`,
`product-ai-feature-poc`, `product-ai-feature-production`,
`business-value-domain-ai-poc`) all sit at the advanced end of the developer
group; the three below are the ones that build most directly on what she's
already doing.

## 1. Team/Product context advanced (e.g. MCPs, memory, skills, orchestration)

> ...when your setup grows from .md files to a more complete set of tools
> and practices that enhance your AI development.

**What this could look like for Maaret:** she already has shared `.md`
context running as a team resource at both ORT and MH, and separately runs
a fuller personal toolkit — the Figma/Storybook/Playwright MCP combo,
skills like the accessibility feedback slice and TIM. One way to try this
could be moving one of those personal pieces (the MCP combo, or a skill
like TIM) into ORT or MH's shared team setup, so it's something the whole
team draws on the way they already draw on the shared `.md` files, not just
something she runs solo.

## 2. Building own MCPs

> ...when you need to find ways of allowing AI to talk to your own things,
> and build something to allow for that.

**What this could look like for Maaret:** she already selects and
configures third-party MCPs (the Figma/Storybook/Playwright combination).
A natural next step could be building a small MCP of her own for something
no third-party server covers — for example, wrapping a CGI AI Exchange
metrics feed, or a client's bug-tracking data, so an agent can query it
directly instead of her doing the local summarizing and file-moving by
hand, the way she currently does for public bid work.

## 3. Product AI feature delivery for proof of concept

> ...when you build software system where a feature uses AI, either
> through a harness piling on top existing layers, or through API to an
> LLM. It did not go to production, but you showed AI in a system you
> build, not as way of building the system.

**What this could look like for Maaret:** she's seen this at other clients
but not yet on ORT or MH, and she's already close to it — she generates AI
artifacts, gives AI feedback on product features (ORT's 25k-bug efforts,
the Omni Raha agentic slices), and has delivered AI-native projects
end-to-end. One way to try this could be turning one of those touchpoints
— e.g. the Omni Raha work — into an actual AI-powered feature built into
the product itself, shown working as a proof of concept, as a first step
toward `product-ai-feature-production` later.
